name: Security Scans (Disabled)
# All security scanning steps have been removed.
# Re-enable this workflow when DevSecOps practices are needed.

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Security scans are currently disabled'
        required: false

jobs:
  placeholder:
    name: Security scans disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security scans are disabled. Enable this workflow when needed."